# Session 02 — Backend core: endpoints, Provider client, transactions

> Not started yet. Decision log for `state_machine.py`, `routers/sessions.py`,
> `provider_client.py`/`provider_mock.py`, and go-live's transactional behavior goes here as it
> happens — see `AI_LOG.md` for how this fits into the overall log.

---

_(fill in as Phase 2 happens)_
